# VS Code: Join Path Extension

## Description

The following extension is used to verify the `vscode.Uri.joinPath` API.

## Contributes:

### Command

`vscode-join-uri.test`: prints a message after performing a `joinPath` operation:

```
file:///foo/bar
```
